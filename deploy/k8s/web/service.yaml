apiVersion: v1
kind: Service
metadata:
  name: osint-web
  namespace: osint
  labels:
    app: osint-web
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: osint-web
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
